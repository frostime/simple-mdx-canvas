---
title: "simple-mdx-canvas Example Report"
description: "A single-page MDX canvas generated from Markdown plus Bulma-based components."
theme: "default"
data:
  componentRows:
    $inline:
      - { component: "Chart", purpose: "Visualize numeric data", basis: "Chart.js" }
      - { component: "Table", purpose: "Show structured rows", basis: "Bulma table" }
      - { component: "Tag", purpose: "Compact labels", basis: "Bulma tag" }
      - { component: "Grid", purpose: "Dense layout", basis: "Bulma grid" }
  complexityCfg:
    $inline:
      type: bar
      data:
        labels: [Markdown, MDX Canvas, Document site, Full React app]
        datasets:
          - { label: Complexity, data: [1, 3, 7, 9] }
      options:
        scales:
          y: { beginAtZero: true }
---

# simple-mdx-canvas Example Report

<Callout type="summary" title="Core idea">
`simple-mdx-canvas` turns an agent-written Markdown document into a Bulma-styled HTML canvas without requiring a full frontend project.
</Callout>

<Tags>
<Tag color="primary" light>MDX</Tag>
<Tag color="success" light>Bulma</Tag>
<Tag color="info" light>Single HTML</Tag>
</Tags>

## Why this exists

Plain Markdown is portable, but it is weak at visual comparison, guided reading, and report-style presentation. A full React app is too heavy for a one-off agent artifact.

<Columns>
<Column title="Plain Markdown">

- Low cost
- Easy to diff
- Weak visual structure
- No controlled components

</Column>
<Column title="MDX Canvas">

- Still text-first
- Adds visual components
- Generates direct HTML
- Agent can validate and repair

</Column>
</Columns>

## Component surface

<Grid columns={3}>
<Cell>
<Callout type="tip" title="Layout">
Use `Columns` for simple prose comparison and `Grid` for denser card-like regions.
</Callout>
</Cell>
<Cell>
<Callout type="definition" title="Data">
Use `Table` for JSON-backed tabular data and GFM tables for ordinary prose tables.
</Callout>
</Cell>
<Cell>
<Callout type="warning" title="Escape hatch">
Use raw Bulma HTML only when a semantic component does not exist yet.
</Callout>
</Cell>
</Grid>

<Table
  from="componentRows"
  columns='[
    { "key": "component", "label": "Component" },
    { "key": "purpose", "label": "Purpose" },
    { "key": "basis", "label": "Basis" }
  ]'
/>

<HtmlBlock>

```html
<div class="notification is-info is-light"><strong>Bulma HTML:</strong> HtmlBlock injects raw HTML children.</div>
```

</HtmlBlock>

## Formula support

Inline math is supported by default, for example $C = C_{content} + C_{layout} + C_{runtime}$.

Block math is also supported:

$$
\mathrm{CanvasValue} = \frac{\mathrm{visual\ clarity} + \mathrm{agent\ reliability}}{\mathrm{setup\ cost}}
$$

## Complexity comparison

<Chart
  title="Output path complexity"
  description="Illustrative scale: lower is simpler."
  from="complexityCfg"
/>

## Code highlighting

```ts
export function renderCanvas(input: string): string {
  return input.trim()
}
```

## Runtime behavior

<Tabs>
<Tab title="Render">

```bash
smc render examples/report.mdx -o examples/report.html
```

</Tab>
<Tab title="Serve">

```bash
smc serve examples/report.mdx --port 4321 --open
```

</Tab>
</Tabs>

## Recommended workflow

1. Write one `.mdx` file using GFM Markdown and registered Bulma-based components.
2. Run `smc validate report.mdx` and fix any structured errors.
3. Run `smc render report.mdx -o report.html`.

## Reusable prompt

<PromptBox title="Agent instruction">

```text
Write a single-page MDX canvas document. Use only registered simple-mdx-canvas components. Validate and render it before returning the final paths. This intentionally long instruction line should wrap inside PromptBox instead of forcing horizontal overflow or making the canvas unreadable.
```

</PromptBox>
